app:
  name: 'go-clean-architecture'

api:
  http:
    host: localhost
    port: 8080

database:
  connectionString: "postgres://user:password@localhost:5432/go_clean_arch?sslmode=disable"
  maxOpenConnections: 0 #0 = unlimited
  maxIdleConnections: 2